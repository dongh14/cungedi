Place your local PMTiles basemap archive here as:

- `public/maps/base.pmtiles`

You can override that same-origin public path with:

- `NEXT_PUBLIC_PM_TILES_BASEMAP_PATH=/maps/another-file.pmtiles`

Do not commit a large production PMTiles archive automatically.
